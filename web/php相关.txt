$_GET是一个数组，数组中每一个值都是一个字符串类型

PHP弱类型比较表
	==松散比较：类型转换
	# $a==0	--> 若a为int，直接与0比较
		--> 若不是int，先转换成int再与0比较
			# 将不含数字的字符串转int时，值为0
			# 当一个前半部分为数字的字符串转int时，保留前半部分的数字作为最终的值
			# 将非0的字符串转换为bool型，值为true
			# 数组永远大于int类型的数字
	# https://www.php.net/manual/zh/types.comparisons.php
	# 一切输入都是可疑的，永远不要相信在弱类型的php下任何比较函数、数学运算
	# 否则，你绝对是被php出卖的那一个

网站常见备份文件.git, .svn, .swp, .~, .bak, .bash_history, .bkf